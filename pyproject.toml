[project]
name = "quant-autoresearch"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "alpaca-py>=0.35.0",
    "duckdb>=1.2.0",
    "pandas>=2.2.0",
    "numpy>=2.0.0",
    "pyyaml>=6.0",
    "python-dotenv>=1.0.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
]
